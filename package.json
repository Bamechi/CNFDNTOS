{
  "name": "cnfdnt-os",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/domain.js && node --check lib/google.mjs && node --check lib/output.mjs"
  },
  "engines": {
    "node": ">=22.13"
  }
}
