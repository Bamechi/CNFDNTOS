const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:1680,height:1000},reducedMotion:'reduce'});const errors=[];p.on('pageerror',e=>errors.push(e.message));try{
await p.addInitScript(()=>{window.SpeechRecognition=class{start(){queueMicrotask(()=>this.onstart?.())}stop(){queueMicrotask(()=>this.onend?.())}abort(){this.stop()}}});
await p.goto('http://localhost:4313');await p.getByLabel('Email address').fill('amechi@addcolormedia.com');await p.getByLabel('Password',{exact:true}).fill('vanta');await p.locator('#login-form .primary').click();await p.locator('#hold').waitFor();assert.equal(await p.locator('#voice-status').innerText(),'');assert.equal(await p.locator('#type-instead').innerText(),'OR WRITE');await p.screenshot({path:'/tmp/cnfdnt-cinematic-intake.png'});const box=await p.locator('#hold').boundingBox();await p.mouse.move(box.x+box.width/2,box.y+box.height/2);await p.mouse.down();await p.waitForTimeout(650);assert.equal(await p.locator('#voice-status').innerText(),'...LISTENING...');await p.screenshot({path:'/tmp/cnfdnt-cinematic-listening.png'});await p.mouse.up();await p.waitForTimeout(100);assert.equal(await p.locator('#voice-status').innerText(),'');
await p.getByRole('button',{name:'Open home portal'}).click();if(await p.locator('html').getAttribute('data-theme')!=='dark')await p.locator('#theme-toggle').click();await p.waitForTimeout(200);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.screenshot({path:'/tmp/cnfdnt-cinematic-dark.png'});await p.locator('#theme-toggle').click();await p.waitForTimeout(200);await p.screenshot({path:'/tmp/cnfdnt-cinematic-light.png'});
await p.locator('.world-menu').first().click();await p.locator('#world-form [name=color]').waitFor();await p.locator('.modal-close').click();await p.locator('.world-card-main').first().click();const tabs=await p.locator('.world-tabs button').allTextContents();assert.ok(tabs.indexOf('Rocks')<tabs.indexOf('Projects'));
await p.evaluate(async()=>{const {FocusMusic}=await import('/music.js');const original=FocusMusic.prototype.level;FocusMusic.prototype.level=function(){const value=original.call(this);window.musicDebug={value,playing:this.playing,time:this.audio.currentTime,duration:this.audio.duration,context:this.context?.state,gain:this.gain?.gain.value,envelope:this.envelope};return value}});await p.locator('#music-control').click();assert.equal(await p.locator('#music-track option').count(),9);await p.locator('#music-track').selectOption({label:'God Frequency'});await p.locator('#music-play').click();await p.getByText('Playing · Loop on',{exact:true}).waitFor();await p.waitForFunction(()=>document.querySelectorAll('.music-meter i.lit').length>0,{},{timeout:15000});await p.locator('#music-volume').fill('0.22');assert.equal(await p.locator('#music-percent').innerText(),'22%');await p.screenshot({path:'/tmp/cnfdnt-cinematic-music.png'});await p.locator('#music-track').selectOption({label:'Curious Horizons'});await p.waitForTimeout(1600);await p.locator('#music-play').click();await p.getByText('Paused.',{exact:true}).waitFor();await p.waitForTimeout(150);assert.equal(await p.locator('.music-meter i.lit').count(),0);await p.setViewportSize({width:390,height:844});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.screenshot({path:'/tmp/cnfdnt-cinematic-mobile-music.png'});await p.locator('.modal-close').click();await p.locator('.mobile-intake-logo').click();await p.getByRole('button',{name:'Open home portal'}).click();await p.screenshot({path:'/tmp/cnfdnt-cinematic-mobile.png'});assert.deepEqual(errors,[]);console.log('PASS Cinematic: exact intake copy, listening state, desktop themes, phone overflow, World customization/tabs, all eight tracks, real audio levels, volume and fades.');
}catch(e){console.log('AUDIO:',await p.evaluate(()=>window.musicDebug));console.log('UI:',(await p.locator('body').innerText()).slice(-2500),'ERRORS:',errors);await p.screenshot({path:'/tmp/cnfdnt-cinematic-failure.png'});throw e}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
